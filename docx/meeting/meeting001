
## 会议记录

### 1. 背包功能定位
- 最初未明确“背包”功能的具体作用。
- 经过讨论一致决定：将“背包”定位为“收藏”功能。
- 也就是说，用户可将感兴趣的精灵/技能加入背包，作为收藏列表使用。

### 2. 后端 API 设计问题
- 发现后端将头像访问接口设计成需要 JWT 认证。
- 该设计不合理：浏览器无法直接通过图片链接访问头像资源。
- 决议：
  - 头像资源访问应支持公开访问或拆分认证逻辑
  - 头像 URL 不应直接依赖 JWT 验证，避免前端显示异常

### 3. 密码存储方案
- 虽然这是一个“大学项目”，但为了安全性仍然决定采用密文存储
- 不使用明文密码
- 后端应实现密码加密/散列存储，如 bcrypt、SHA-256+盐 等安全方案

### 4. 结论
- 背包 = 收藏功能
- 修正头像访问 API 设计，避免 JWT 强制认证影响浏览器链接访问
- 密码必须加密存储，保证安全性
